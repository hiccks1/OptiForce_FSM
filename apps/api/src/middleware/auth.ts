// apps/api/src/middleware/auth.ts - Auth middleware placeholder

export const DRIFTY_FILE_CONTRACT = {
  driftyVersion: "1.0.0",
  layers: ["L3_INTEGRATION"],
};

export function auth(req:any,res:any,next:any){ next(); }
